# 项目记忆

## 项目定位

- 项目名称：user_insight_pm v1.0
- 创建日期：2026-06-05
- 类型：无构建步骤的静态前端原型
- 入口：`index.html`

## 运行链路

- `index.html`：密码访问入口。
- `main.html`：原型导航和 iframe 容器。
- `pages/userinsight.html`：用户洞察业务页面。
- `pages/workbench.html`：电销员接收培育任务后使用的客户培育工作台。
- `docs/interaction.html`：交互说明文档。

## 代码结构

- `assets/css/app.css`：用户洞察样式入口，按顺序导入拆分后的样式模块。
- `mock/data.js`：业务 Mock 数据及兼容别名。
- `js/common.js`：公共工具。
- `js/nav.js`：页面导航。
- `js/pages/userinsight/`：按筛选、旅程、弹窗、渲染、门店、下载和统计拆分的业务逻辑。
- `js/app.js`：页面初始化入口。
- `assets/css/workbench.css`：培育工作台及预测画像样式。
- `js/pages/workbench.js`：工作台任务切换、预测画像和展开面板交互。
- `annotations/`：标注数据、样式和运行时。

## 工作原则

- 先理解现有代码和业务逻辑，再修改。
- 非必要不修改现有板块内容。
- 业务、Mock、文档、记忆和标注工具分别维护。
- 保持脚本加载顺序，确保 Mock 和页面函数在初始化入口之前可用。
- 不要凭空猜测业务口径（标签名、筛选条件、计算规则），代码中没有时必须确认。
- 优先建立通用检查（验证脚本、通用化函数）而非逐个修复问题。
- 发现问题时同步检查三个 Tab（渠道效果/培育运营/用户群体洞察）。
- 修改 HTML 结构前定位到具体行号和元素 ID，改完运行 `node tools/validate-project.js`。
- 用户说"内容在文件里"时先搜索特征词，范围包括注释、隐藏元素和 git 历史。
- 项目记忆优先更新到项目文件夹 `memory/`，而非个人目录 `~/.claude/projects/`。

## 当前重点交互

- 触媒/触店习惯中的「首次到末次留资渠道」「首次到末次留资媒体」「首触-成交专营店」均以一级入口 + 二级右侧抽屉桑基图呈现。
- 培育运营的总部培育跟进过程和门店虚拟号跟进过程使用数据列表式过程分析，不显示统计柱；呼叫线索三段同一行，接通线索左右等分，并在接通次数/接通时段中展示呼叫次数。
- 桑基图实现使用原生 SVG，不引入 ECharts 等新依赖。
- 大量媒体/专营店的「全部」视图采用分页渲染，避免一次性生成过高 SVG 导致页面空白。
